[build]
builder = "NIXPACKS"
nixpacksConfigPath = "nixpacks.webapp.toml"

[deploy]
startCommand = "NODE_ENV=production node --max-old-space-size=400 --expose-gc api/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
